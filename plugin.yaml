name: mongreldb_hermes
version: 1.0.1
kind: exclusive
description: "MongrelDB-backed long-term memory for Hermes. Supports native FFI (default) and HTTP daemon modes, with optional dense ANN."
author: Visorcraft
hooks:
  - on_session_end
