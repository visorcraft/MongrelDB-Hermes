name: mongreldb_hermes
version: 1.3.0
kind: exclusive
description: "MongrelDB-backed long-term memory for Hermes. Supports native FFI and HTTP daemon modes, with dense ANN by default and sparse opt-out."
author: Visorcraft
hooks:
  - on_session_end
